//! This example relies on tracing which is not yet fully ported.
use autozig_app::prelude::*;

fn main() {
    println!("Log layers example requires tracing support, which is currently a placeholder.");
    App::new().run();
}
