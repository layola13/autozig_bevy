[package]
name = "autozig-app"
version = "0.1.0"
edition = "2021"

[dependencies]
autozig = { path = "../../autozig" }
autozig-ecs = { path = "../autozig-ecs" }
autozig-reflect = { path = "../autozig-reflect" }


[build-dependencies]
autozig-build = { path = "../../autozig/gen/build" }

[dev-dependencies]